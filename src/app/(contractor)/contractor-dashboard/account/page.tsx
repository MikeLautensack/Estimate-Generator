import { Typography } from "@mui/material";
import React from "react";

const page = () => {
  return (
    <main>
      <Typography color="primary" className="">
        Account
      </Typography>
    </main>
  );
};

export default page;
