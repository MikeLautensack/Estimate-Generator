import { Typography } from "@mui/material";
import React from "react";

const page = () => {
  return (
    <main>
      <Typography color="primary" className="">
        Settings
      </Typography>
    </main>
  );
};

export default page;
