import { Typography } from "@mui/material";
import React from "react";

const page = () => {
  return (
    <main>
      <Typography color="primary" className="">
        Profile
      </Typography>
    </main>
  );
};

export default page;
